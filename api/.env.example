NODE_ENV=development
PORT=3000
DATABASE_URL=postgresql://app:app@localhost:5432/app?schema=public
CORS_ORIGIN=http://localhost:5173
# Genera el tuyo: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Mínimo 32 caracteres. Nunca reutilizar este valor de ejemplo en un ambiente real.
JWT_SECRET=cambia-este-valor-con-uno-generado-de-al-menos-32-caracteres
